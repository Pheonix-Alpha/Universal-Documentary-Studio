# Core
# Pinned below 6.0: Gradio 6 moved `theme=` from Blocks(...) to .launch(...),
# so an unbounded version here could silently start ignoring the theme (or
# worse, break) depending on when `pip install` resolves it in a fresh Colab.
gradio>=4.0.0,<6.0.0
numpy
Pillow
requests

# AI/ML
torch>=2.0.0
transformers
diffusers
accelerate
huggingface_hub

# Video generation (GPU-heavy)
# For Stable Video Diffusion
xformers  # Optional for performance

# For video assembly
opencv-python-headless
moviepy

# For worker-server
fastapi
uvicorn

# API clients
anthropic
google-generativeai
openai

# Search and retrieval
duckduckgo-search